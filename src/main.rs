/**
 * This is the setup code for the SvelteUI cli.
 * 
 * There is nothing actually useful yet, however that will change soon
 */
mod core;

fn main() {
    let placeholder: String = String::from("I am a placeholder");

    println!("{}", placeholder);
}